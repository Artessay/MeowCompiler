// #define MaxN 10000

// int scanf(char *__format, ...);

int printf(char *__format, ...);

int main() {
    int T = 10;
    while (T != 0) {
        printf("%d\n", T);
        T = T - 1;
    }
    return 0;
}